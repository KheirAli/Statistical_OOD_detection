# X-ray pilot, DDAD-native conditioned denoising.
# EDIT samples + num_seeds to match tools/prepare_xray_dataset.py output.
sampler:
  name: ddad_native
  params: {w: 2.0, test_trajectoy_steps: 250, skip: 25, eta: 1.0, trajectory_steps: 1000, beta_start: 0.0001, beta_end: 0.02}

ckpt: /data/akheirandish3/ddad_xray_ckpts/MVTec/xray/500           # EDIT: post-training epoch
image_dir: /data/akheirandish3/xray_mvtec/test/prohibited
samples: []    # EDIT: populate after Phase A (e.g. ["P00001", "P00002", ...])
num_seeds: 20
out_root: ./results_patches_xray_native
bottom_suffix: "4"
